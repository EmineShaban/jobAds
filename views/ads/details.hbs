<main>

    <section id="details-page">
        <article class="details-gage-title">
            <h1>Details Page</h1>
        </article>
        <article class="details-page-card">

            <article class="details-page-card-image">
                <!-- Do not forget to change the path to the image -->
                <img src="/img/man.png" alt="details-image">
            </article>

            <article class="details-page-card-content">
                <p>Author: {{ownerAds.email}}</p>
                <h2>Headline: {{headline}}</h2>
                <h3>Company name: {{companyName}}</h3>
                <h4>Company description: {{description}}</h4>
                <h5>Location: {{location}}</h5>

                <article class="details-page-card-buttons">
                    {{#if user}}
                    {{#if isAuthor}}
                    <!--only for registered users and author of the ad-->
                    <a href="/ads/{{_id}}/edit" class="btn-edit">Edit</a>
                    <a href="/ads/{{_id}}/delete" class="btn-delete">Delete</a>
                    {{else}}
                    {{#if isAlreadyJoin}}
                       <!--logged in user who has already applied for current ad-->
                    <p class="already-applied">You have already applied!</p>
                    
                    {{else}}
                 <!--logged in user who did not apply for current ad-->
                    <a href="/ads/{{_id}}/join" class="btn-apply">Apply now!</a>
                    <p class="applied-people">Hurry up, {{count}} people have already applied.</p>
                    {{/if}}
                    {{/if}}
                    {{/if}}
                </article>
            </article>

        </article>
    </section>
    {{#if isAuthor}}

    <!--This section should only be visible to the author of the current ad-->
{{#if usersApplied.length}}
    <section id="info-about-candidates">
        <h2>Candidates for this ad:</h2>
        <!--If there are people who have applied, show each of them in the following format-->
{{#each usersApplied}}
        <article class="info-about-candidates-card">
            <h2>Email: {{email}}</h2>
            <h3>Description of the candidate's skills: {{description}}</h3>
        </article>
        {{else}}
               <h2>aaaaa</h2>
   {{/each}}

        <!--If not, you don't need to visualize anything further-->
    </section>
    {{/if}}
    {{/if}}

</main>




{{!-- <section id="viewhotelDetails">
    <h2>Details</h2>
    <div class="hotel-ticket">
        <div class="hotel-left">
            <img src="{{imgUrl}}" alt="imgUrl">
        </div>
        <div class="hotel-right">
            <div>
                <h3>{{hotel}}</h3>
            </div>
            <div>
                {{city}}
            </div>
            <p><span>Free rooms: {{freeRooms}}</span> </p>



            {{#if isAuthor}}
            <a href="/booking/{{_id}}/edit" class="edit">Edit</a>
            <a href="/booking/{{_id}}/delete" class="remove">Delete</a>
            {{else}}
            {{#if isAlreadyJoin}}
            <p><span class="green">You already have booked a room</span> </p>
            {{else}}
            <a href="/booking/{{_id}}/join" class="book">Book</a>
            {{/if}}
            {{/if}}
        </div>

    </div>
</section> --}}